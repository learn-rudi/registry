interface:
  display_name: "RUDI Code Review"
  short_description: "Review Standards, Spec, and Proof independently"
  default_prompt: "Use $rudi-code-review to inspect this bounded change independently and return prioritized findings plus separate Standards, Spec, and Proof verdicts."
