interface:
  display_name: "RUDI Repo Steward"
  short_description: "Steward every Git repo beneath one folder"
  default_prompt: "Use $rudi-repo-steward to enroll this folder, discover its Git repositories, and propose the next safe targeted actions."
